/**
 * Настройки окружения.
 *
 * apiUrl — адрес бэкенда. Пока нигде не используется: запросы к API
 * появятся на следующем этапе, а здесь адрес лежит заранее, чтобы он
 * был в одном месте, а не размазан по коду компонентов.
 */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080',
};
